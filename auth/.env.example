# ---- Auth service (Better Auth + Hono + SQLite) ----
PORT=4000

# Optional envs (defaults shown):
# AUTH_PORT=4000
# AUTH_BASE_URL=http://localhost:5173
# BETTER_AUTH_SECRET=generate-one-with-openssl-rand-base64-32
# AUTH_TEST_EMAIL=test@docu.com
# AUTH_TEST_PASSWORD=password